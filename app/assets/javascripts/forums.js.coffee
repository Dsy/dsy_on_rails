# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://jashkenas.github.com/coffee-script/

#$(document).ready ->
#jQuery ->
    #$("#threads").ajaxSuccess (event, xhr, settings) ->
        #alert("First AJAX SUCCESS!!!")
#        console.log("error json: ",  jQuery.parseJSON(xhr.responseText));

#jQuery ->
    #$("nav.pagination > span.page").click ->
        #alert("pa-gi-na-tion!")
#jQuery ->
    #$('#paginator').html(escape_javascript(paginate(@threads, :remote => true).to_s));
